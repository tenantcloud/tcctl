The commands with which we can update and restart servicectl

for example:

tcctl service servicectl OS

where:

servicectl              - the name of service which we want to update and restart
OS                      - type of OS
