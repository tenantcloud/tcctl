The commands with which we can wait replicas to zero lag

for example:

tcctl aws wait_replicas_zero_lag --db-master-name DB_MASTER_NAME

where:

DB_MASTER_NAME          - the value of the variable DB_HOST from the .env project file
