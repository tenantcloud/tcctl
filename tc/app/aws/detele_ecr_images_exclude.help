The commands delete old images from AWS ECR exclude specified tag

for example:

tcctl aws detele_ecr_images_exclude --profile ecr --repository km/test --leave-last-image 5 --exclude-tag 503

where:

--profile          - profile name in ~/.aws/credentials
--repository       - repository name in AWS ECR
--leave-last-image - images count which will be leave after delete
--exclude-tag      - tag name which will be excluded
