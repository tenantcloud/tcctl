The commands with which we can get db identifier

for example:

tcctl aws get_db_identifier --db-master-name DB_MASTER_NAME

where:

DB_MASTER_NAME          - the value of the variable DB_HOST from the .env project file
