The commands with which we can promote master replicas

for example:

tcctl aws promote_master_replicas --db-master-name DB_MASTER_NAME

where:

DB_MASTER_NAME          - the value of the variable DB_HOST from the .env project file
