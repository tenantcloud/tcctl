The command which get value of the variable from the .env project file

for example:

tcctl helpers env get --key APP_ENV --env .env

where:

APP_ENV                 - the variable from the .env project file
.env                    - project file in which we work
