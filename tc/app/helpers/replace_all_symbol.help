
tcctl helpers replace_all_symbol --find-symbol "SYMBOL1" --replace-symbol "SYMBOL2" --string-to-edit "STRING"

Require:

--find-symbol             "the symbol to replace
--replace-symbol          "the symbol to which we replace"
--string-to-edit          "the string which we want to edit"
