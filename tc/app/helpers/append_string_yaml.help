
tcctl helpers append_string_yaml --yaml-path "PATH" --yaml-value "VALUE" --file-name "FILE NAME"

Require:

--yaml-path          "path to value in the yaml file"
--yaml-value         "value to insert"
--file-name          "the name of yaml file"
