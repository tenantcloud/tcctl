The commands with which we can check status of service

for example:

tcctl service status --service-name service_name

where:

service_name            - the name of service which we want to update and restart
