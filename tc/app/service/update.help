The commands with which we can update servicectl

for example:

tcctl service update servicectl OS

where:

servicectl              - the name of service which we want to update and restart
OS                      - type of OS
