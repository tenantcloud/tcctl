The commands with which we can send slack message with solved issue list from JIRA

for example:

tcctl slack solved_issue_list --channel "#devops"

where:

channel            - the name of Slack channel
