The commands with which we can send slack message

for example:

tcctl slack pr_information --channel "#devops" --url "$URL" --pull-request-url "PULLREQUEST_URL"

where:

channel            - the name of Slack channel
url                - variable website link from build script
pull-request-url   - variable pull request link from build script
