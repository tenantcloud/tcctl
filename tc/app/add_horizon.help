The command with which we can add php artisan horizon process

for example:

tcctl add_horizon PROJECT_DIR_NAME PROJECT_NAME OS

where:

PROJECT_DIR_NAME        - full path to the project directory
PROJECT_NAME            - project name
OS                      - type of OS
