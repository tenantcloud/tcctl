#!/usr/bin/env bash

source "$CLI_DIR/tc/framework/functions"

__output=$( php artisan $@ 2>&1 )
export _error_code=$?
__MSG="Command: \033[36mphp artisan $@\033[39m\n"
__MSG+="$__output"
if [ $_error_code -eq "0" ]; then
  echo "$__output"
else
  _message_error "$__MSG"
fi
