#!/usr/bin/env bash

function vault_put() {
  tcctl vault token
  VAULT_SECRETS=$1
  ENV_FILE=$2
  MSG="Put to vault"
  vault kv put $VAULT_SECRETS $(egrep -v '^#' $ENV_FILE | xargs) \
  || tcctl vault messageError "Error. $MSG"
}

vault_put $1 $2
