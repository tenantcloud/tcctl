#!/usr/bin/env bash

function vault_diff() {
  ENV_FILE=$1
  ENV_TEMP=$2
  diff -y --suppress-common-lines $ENV_TEMP $ENV_FILE | column -t \
  || tcctl vault messageError "Error. $MSG"

}

vault_diff $1 $2
