#!/usr/bin/env bash

function vault_diff() {
  ENV_FILE=$1
  ENV_TEMP=$2
  DIFF=$(diff -y --suppress-common-lines $ENV_TEMP $ENV_FILE | column -t)
  echo "$DIFF"
  tcctl vault remove $ENV_TEMP
}

vault_diff $1 $2
