#!/usr/bin/env bash

function vault_check_path() {
  tcctl vault token
  MSG="Get path from vault server"
  VAULT_SECRETS=$1
  vault kv get $VAULT_SECRETS | sed 1,8d | head -1 \
  || tcctl vault messageError "Error. $MSG"
}

vault_check_path $1
