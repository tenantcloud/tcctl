The command which put the keys to the vault server

for example:

tcctl vault put --secret-path projects_name/dev/projects_brahch --env .env - for use by developers

tcctl vault put --secret-path projects_name/APP_ENV --env .env - for use by servers

projects_name           - project name in git repository
projects_brahch         - current project branch
APP_ENV                 - the value of the variable from the .env project file
.env                    - project file in which we work
