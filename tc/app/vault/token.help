ARGS  - The arguments you wish to provide to this command

The command which generates a temporary user token regarding its credentials
and the one only works after execution tcctl vault config http://127.0.0.1:8200 username userpassword

for example:

tcctl vault token
