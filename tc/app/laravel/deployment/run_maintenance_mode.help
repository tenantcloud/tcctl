The commands with which we can run maintenance mode

for example:

tcctl laravel deployment run_maintenance_mode --delay_time 2 --terminate-when-finish true

where:

delay_time              - delay time before run maintenance mode
terminate-when-finish   - true
