The commands with which we can deactivate maintenance mode

for example:

tcctl laravel maintenance off \
    --url "www.google.com" \
    --token "secret_token_for_api_calls"

where:

url		- (required variable) url where maintenance mode must be deactivate
token           - (optional variable) access token for command accept
