
tcctl laravel pull-request \
  --bitbucket-username "NAME" \
  --repo-slug "SLUG" \
  --branch-source "BRANCH1" \
  --branch-destination "BRANCH2"
  --bitbucket-client-id "ID" \
  --bitbucket-client-secret "SECRET"

Require:

--bitbucket_username        "username"
--repo-slug                 "repo name"
--branch-source             "branch from"
--branch-destination        "branch to"
--bitbucket-client-id       "id"
--bitbucket-client-secret   "secret"
